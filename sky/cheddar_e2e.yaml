name: heir-cheddar-dbg
workdir: .
run: |
  CC=clang bazel test //tests/Examples/cheddar/mnist:mnist_test \
    --//:enable_cheddar=1 --test_output=all --test_timeout=3600 \
    --test_tag_filters= 2>&1 | tail -80
  echo "## DONE ##"
