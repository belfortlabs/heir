# SkyPilot config for running CHEDDAR backend tests in default (debug)
# compilation mode on a GPU instance. Mirrors sky/cheddar_test.yaml but drops
# `-c opt` so the bazel cache on this instance is warmed for debug iteration.
#
# Usage: sky launch -c heir-cheddar-dbg -i 10 sky/cheddar_test_dbg.yaml
#
# Uses nvidia/cuda Ubuntu 24.04 base image for clang-18 + CUDA compatibility.

name: heir-cheddar-dbg

# assumes we'll call this from the repo root
workdir: .

resources:
  accelerators: A10G:1
  cpus: 64+
  memory: 128+
  disk_size: 200
  cloud: aws
  image_id: docker:nvidia/cuda:12.9.1-cudnn-devel-ubuntu24.04
  autostop:
    idle_minutes: 10

setup: |
  set -ex

  apt-get update -qq
  apt-get install -y -qq \
    clang lld libomp-dev llvm \
    zlib1g-dev \
    libtommath-dev libjsoncpp-dev \
    libcusparselt0 \
    git wget pip

  if ! command -v bazel &> /dev/null; then
    wget -q https://github.com/bazelbuild/bazelisk/releases/latest/download/bazelisk-linux-amd64 -O /usr/local/bin/bazel
    chmod +x /usr/local/bin/bazel
  fi

# Note: if we don't fetch first, we might see random timeouts from things such as python wheel extraction/etc.
run: |
  set -ex
  bazel fetch //... --//:enable_cheddar=1
  CC=clang bazel test \
    //tests/Examples/cheddar/... \
    --//:enable_cheddar=1 --test_output=streamed
